module.exports = {
    // ...
    /* comment me back in after running container. Attached a shell to the running instance (easy in vs code)
    // then:
    // cd /opt/app/src/plugins/arse && npm install && npm run build
    // then you can docker compose down, recomment this code, and docker compose up
    'arse': {
      enabled: true,
      resolve: './src/plugins/arse'
    },
    */
    // ...
  }